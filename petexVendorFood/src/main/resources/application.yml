server:
  port: 1112
spring:
  servlet:
    multipart:
      max-file-size: 1GB  # Set the maximum file size to 1GB
      max-request-size: 1GB  # Set the maximum request size to 1GB
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    password: Rajesh@6301259439
    url: jdbc:mysql://localhost:3306/petexproject
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
